
SHELL		= @SHELL@
TCLSH		= @TCLSH@
srcdir		= @srcdir@
prefix		= @prefix@
exec_prefix	= @exec_prefix@
bindir		= @bindir@
libdir		= @libdir@
datadir		= @datadir@
datarootdir	= @datarootdir@
mandir		= @mandir@
includedir	= @includedir@

pkgdatadir	= $(datadir)/$(PKG_DIR)
pkglibdir	= $(libdir)/$(PKG_DIR)
pkgincludedir	= $(includedir)/$(PKG_DIR)
top_builddir	= .

MKDIR_P		= @MKDIR_P@
INSTALL		= @INSTALL@

SCRIPTS	= \
		encodedata \
		$(srcdir)/launcher.tcl \
		$(srcdir)/grabdata \
		$(srcdir)/nanovis-test \
		$(srcdir)/vtkcontour-test \
		$(srcdir)/vtkstreamlines-test \
		$(srcdir)/vtkviewer-test \
		$(srcdir)/flowvis-test \
		rappture \
		rappture.env \
		rappture-csh.env \
		rappture.use \
		rerun \
		rpdiff \
		simsim  \
		xmldiff \
		about

WINDOWS_SCRIPTS =  \
		$(srcdir)/rappture.cmd \
		$(srcdir)/driver.cmd \
		$(srcdir)/rerun.cmd

all:

install:
	$(MKDIR_P) -m 0755 $(bindir)
	@for i in $(SCRIPTS); do \
	    echo "Installing $$i" ; \
	    $(INSTALL) -m 0555 $$i $(bindir) ; \
	done

clean:

distclean: clean
	$(RM) encodedata rappture.env rappture rappture.use rerun rpdiff simsim xmldiff
	$(RM) Makefile *~

